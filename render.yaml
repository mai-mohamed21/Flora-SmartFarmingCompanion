services:
  - type: web
    name: plant-disease-api
    env: python
    plan: free
    buildCommand: |
      cd Backend
      pip install -r requirements.txt
    startCommand: |
      cd Backend
      gunicorn app:app --bind 0.0.0.0:$PORT
